# Optional SMTP settings. If EMAIL_HOST is blank, Django prints notifications to the terminal.
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-user
EMAIL_HOST_PASSWORD=your-password
EMAIL_USE_TLS=1
